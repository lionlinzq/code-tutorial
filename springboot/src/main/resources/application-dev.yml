spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.tcp.cpolar.top:12359/framework?serverTimezone=UTC
    username: root
    password: 9527

# nacos相关配置
#nacos:
#  config:
#    server-addr: nacos.lionlinzq.top:8848
#    # 开启自动刷新 nacos 配置
#    # 注意，需要使用 @NacosValue 注解，并且 autoRefreshed 必须设置为 true
#    auto-refresh: true
#    # 针对配置项同名的情况，是否允许 nacos 的配置覆盖本地的配置
#    remote-first: true
